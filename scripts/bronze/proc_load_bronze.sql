/*
This is a stored procedure to BULK LOAD (all at once) instead of LOAD (row by row) from source systems (CSV files) to tables in bronze schema
naming format: load_bronze;

Usage example:
  EXEC load.bronze;
*/
